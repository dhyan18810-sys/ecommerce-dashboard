{
  "name": "ecommerce-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "cd client && npm run build",
    "install:all": "cd client && npm install"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
